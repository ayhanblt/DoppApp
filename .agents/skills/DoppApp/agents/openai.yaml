interface:
  display_name: "DoppApp"
  short_description: "Repo-specific patterns and workflows for DoppApp"
  default_prompt: "Use the DoppApp repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true